<div metal:use-macro="load: layout.pt">
    <div metal:fill-slot="content">
        <div class="content">
            <h1><span class="font-semi-bold">auto_svc</span> <span class="smaller">A basic RESTful auto service</span></h1>
            <p class="lead">
                Feel free to use our API to shop for cars<br>
                <br>
                <strong>Endpoints</strong>
            <ul>
                <li>
                    <strong>List all cars</strong><br>
                    <a href="/api/autos">GET /api/autos</a>
                    Returns a JSON body listing car details
                </li>
                <li>
                    <strong>Individual car details</strong><br>
                    <a href="/api/autos/123">GET /api/autos/{car_id}</a>
                    Returns a car's name, price, year, brand ... as  JSON
                </li>
                <li>
                    <strong>Add new car</strong><br>
                    <a href="/api/autos">POST /api/autos}</a>
                    Create a new car based on the details supplied as a JSON body.
                </li>
                <li>
                    <strong>Update a car's details</strong><br>
                    <a href="/api/autos/123">PUT /api/autos/{car_id}</a>
                    If a car's details already contain a price, year, etc. update those details based on supplied as a JSON body.
                </li>
                <li>
                    <strong>Delete a car</strong><br>
                    <a href="/api/autos/123">DELETE /api/autos/{car_id}</a>
                    Delete the record for a given car based on its supplied ID.
                </li>
            </ul>
            </p>
        </div>

    </div>
</div>
